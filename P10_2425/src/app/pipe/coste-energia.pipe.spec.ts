import { CosteEnergiaPipe } from './coste-Enegia.pipe';

describe('costeEnergia', () => {
  it('create an instance', () => {
    const pipe = new CosteEnergiaPipe();
    expect(pipe).toBeTruthy();
  });
});
